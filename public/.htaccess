RewriteEngine On

# Si fichier/dossier existe, on sert direct
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon on redirige vers index.php
RewriteRule ^ index.php [QSA,L]
